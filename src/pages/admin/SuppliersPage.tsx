import { Users } from "lucide-react";
import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function SuppliersPage() {
  return (
    <PagePlaceholder
      icon={Users}
      title="Suppliers"
      description="Supplier accounts with Amount / Given / Balance, detail editing, and merge come in Phase 2."
    />
  );
}
