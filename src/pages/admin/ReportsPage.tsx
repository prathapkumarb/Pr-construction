import { BarChart3 } from "lucide-react";
import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ReportsPage() {
  return (
    <PagePlaceholder
      icon={BarChart3}
      title="Reports"
      description="Day / week / monthly / custom reports with Excel export come in Phase 3."
    />
  );
}
