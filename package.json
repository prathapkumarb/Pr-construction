{
  "name": "flux-1b85e",
  "version": "0.1.0",
  "private": true,
  "description": "Static site (HTML + Tailwind + shadcn style) on Firebase Hosting",
  "scripts": {
    "login": "firebase login",
    "serve": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^13.15.1"
  }
}
