// Firebase web config for project flux-1b85e
//
// NOTE: These values are NOT secret — Firebase web config is meant to be public.
// Access is controlled by Firebase Security Rules, not by hiding these keys.
//
// Get the real values from:
//   Firebase Console > Project settings (gear icon) > General
//   > "Your apps" > Web app > "SDK setup and configuration" > Config
//
// Replace the placeholders below, then run `npm run deploy`.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "flux-1b85e.firebaseapp.com",
  projectId: "flux-1b85e",
  storageBucket: "flux-1b85e.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
